version: 2

#This is like writing SELECT * FROM <<table_name>> from postgreSQL

sources:
    - name: raw
      database: MUSIC_APP_DB
      schema: RAW
      tables:
        - name: playlists #music_app_archive_playlist
        - name: streaming_links #music_app_archive_streaminglink
        - name: tracks #music_app_archive_track
        - name: users #music_app_auth_customuser